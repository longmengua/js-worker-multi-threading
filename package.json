{
  "name": "js-worker-multi-threading",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts src/workers/hourseRace.ts --outdir=dist --platform=node --format=esm",
    "build:watch": "esbuild src/index.ts src/workers/hourseRace.ts --outdir=dist --platform=node --format=esm",
    "start": "node dist/index.js",
    "dev": "npm run build:watch && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "engines": {
    "node": "~22"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "esbuild": "^0.25.0"
  }
}